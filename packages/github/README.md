# `@oexl/github`

Review-first GitHub integration primitives for OEXL deliverables.

The package can turn a bounded patch into a branch and pull request, then link
that proposal to its originating issue. It never merges, deploys, force-pushes,
or writes directly to the default branch.
